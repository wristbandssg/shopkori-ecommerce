<%- include('partials/admin-header') %>

<div class="admin-card text-center py-5">
  <i class="bi bi-cone-striped" style="font-size: 48px; color: var(--admin-pink);"></i>
  <h4 class="mt-3 mb-2"><%= adminPageTitle %></h4>
  <p class="text-muted mb-0">This feature hasn't been built yet — it's being added step by step. Tell us which one you need next and we'll build it.</p>
</div>

<%- include('partials/admin-footer') %>
