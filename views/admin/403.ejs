<%- include('partials/admin-header') %>

<div class="admin-card text-center py-5">
  <i class="bi bi-shield-lock" style="font-size: 48px; color: var(--admin-pink);"></i>
  <h4 class="mt-3 mb-2">Access Denied</h4>
  <p class="text-muted mb-1">
    Your role<% if (typeof roleName !== 'undefined' && roleName) { %> ("<%= roleName %>")<% } %>
    doesn't have permission to open this section.
  </p>
  <p class="text-muted mb-0">Ask an admin with the Role module to grant access under Staff &gt; Roles, or go back to a page you can use.</p>
  <a href="/admin" class="btn btn-brand mt-3"><i class="bi bi-house"></i> Back to Dashboard</a>
</div>

<%- include('partials/admin-footer') %>
